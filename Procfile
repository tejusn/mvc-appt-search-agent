# Define a process type named 'job' for running the MVC check
job: python3 -c "import main; main.run_job_check()"

# Optional: Define the web process if you still want to build for HTTP service sometimes
# web: functions-framework --target=check_mvc_appointments --source=main.py